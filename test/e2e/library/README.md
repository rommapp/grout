# Library fixture

The roms RomM scans for the end to end run. They hold a few bytes of nothing:
grout is being tested on what it does with a library, not on what is in one,
and a real rom could not be committed here anyway.

The folder names are the platform slugs RomM recognises, which is what it
reads the platform from.
